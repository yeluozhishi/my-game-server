package com.whk.rpc.api.provider;

import com.whk.rpc.api.IRpcServer;
import com.whk.serverinfo.Server;

import java.util.List;

public class RpcServerImpl implements IRpcServer {
    @Override
    public List<Server> getServer(int zone) {


        return List.of();
    }
}
